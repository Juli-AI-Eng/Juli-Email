# OpenAI API key (required for the MCP server)
# This is used by the server to generate email content with AI
OPENAI_API_KEY=your_openai_api_key_here

# Server configuration
PORT=3000

# Optional: Set to 'development' for debug logging
NODE_ENV=production

# Note: Nylas credentials are provided by Juli via HTTP headers
# Users configure their Nylas credentials in Juli, not here